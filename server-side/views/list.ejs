<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>文件列表</title>
</head>
<body>
    <ul>
        <% for (let item of fileList) { %>
            <li>
                <%= item.file_key %> | 
                <%= item.file_name %>
            </li>
        <% } %>
    </ul>
</body>
</html>